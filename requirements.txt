# Core library (import ewjepa) only needs PyTorch + NumPy.
torch>=2.4
numpy>=1.24

# Experiment stack (training scripts, evaluation, benchmarks).
# NOTE: on an RTX 5090 (Blackwell / sm_120) install a CUDA >= 12.8 PyTorch build,
# e.g. `pip install torch --index-url https://download.pytorch.org/whl/cu130`.
hydra-core>=1.3
omegaconf>=2.3
wandb>=0.16
stable-worldmodel>=0.1
tqdm>=4.66
matplotlib>=3.7
scikit-learn>=1.3

# SWM PushT environment + Lance I/O
opencv-python-headless>=4.8
imageio>=2.31
lancedb>=0.6
pyarrow>=14.0
pygame>=2.5
pymunk>=6.5
shapely>=2.0

# Tests
pytest>=7.4
